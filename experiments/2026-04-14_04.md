# Experiment: 2026-04-14_04

## Hypothesis

- `sft_output` をベースモデルとして LoRA を追加すると、ベースQwenからのLoRAより性能が改善する可能性がある。

## Change (Only One)

- LoRA適用先のベースを `Qwen/Qwen3-0.6B-Base` から `sft_output` に変更する。

## Fixed Conditions

- Model: `sft_output`（LoRA追加対象）
- Dataset: `xlsum_ja`（`train.jsonl` / `validation.jsonl`）
- Prompt/System prompt: `system_prompt.txt`
- Seed: 既定値のまま（変更なし）
- Decode params: 既定値のまま（変更なし）

## Command

```bash
python sft_lora.py --base-model sft_output
```

## Result

- ROUGE-L Mean: （未記入）
- ROUGE-L Std: （未記入）
- Detail log: （未記入）
- CSV row timestamp: （未記入）

## Interpretation

- `sft_output` 起点LoRAの効果を、Experiment 03（base Qwen起点LoRA）と比較する。

## Next Action

- LoRA設定（`r` / `alpha` / `dropout`）を固定して、公平比較で差分を確認する。
