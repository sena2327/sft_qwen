# Experiment: 2026-04-15_05

## Hypothesis

- SFT性能向上のために、`text -> summary` を1回経由して情報圧縮を学習し、その後 `text -> title` で LoRA 追加学習すると最終ROUGEが改善する。

## Change (Only One)

- 学習フローを「直接 `text -> title`」から「`text -> summary` の中間学習を経由した後に LoRA で `text -> title` 追加学習」に変更する。

## Fixed Conditions

- Model: `Qwen/Qwen3-0.6B-Base`（または比較対象と同一のベース）
- Dataset: `xlsum_ja`（同一分割を使用）
- Prompt/System prompt: `system_prompt.txt`（同一）
- Seed: 既定値のまま（変更なし）
- Decode params: 既定値のまま（変更なし）

## Command

```bash
# Step 1: summary経由のSFT（実装予定）
python sft_sammarize.py

# Step 2: LoRA追加学習
python sft_lora.py --base-model sft_sammary
```

## Result

- ROUGE-L Mean: （未記入）
- ROUGE-L Std: （未記入）
- Detail log: （未記入）
- CSV row timestamp: （未記入）

## Interpretation

- summary経由学習の有無で、LoRA追加学習後の性能差を比較する。

## Next Action

- summary学習用のデータ整形（`text -> summary`）を先に実装し、Step 1 を実行する。
